# デスクトップ運用方針（確定メモ）

更新日: 2026-08-11  
位置づけ: オーナー回答に基づく **運用・中優先改修の確定方針**。実装前の契約。  
関連: [`R1A_DESKTOP_WALKTHROUGH_BACKLOG.md`](R1A_DESKTOP_WALKTHROUGH_BACKLOG.md) / [`R1_DEEP_LOOP_SPEC.md`](R1_DEEP_LOOP_SPEC.md)

---

## 0. 一文で

- **オリジナル**は機種別2文字接頭辞＋日付の統一規則（下記 §2）。  
- **Works** はユーザー自作で **月フォルダ `YYYYMM` のみ**（イベントサブフォルダは作らない）。  
- アプリは **短絡（Rating/説明）＋ DxO修正後の記録 ＋ Works 月フォルダ上の痕跡**だけを担う。生成もコピーもしない。

---

## 1. 維持する現行仕様（一度広げた案は撤回）

| 項目 | 方針 |
|------|------|
| Works 構成 | **月 `YYYYMM` のみ**。イベント `YYYYMMDD_名前` は **作らない** |
| Works パス自動生成 | **しない**（ユーザーが自分で作る） |
| 短絡開始時の Works 生成 | **しない** |
| Works へのファイルコピー | **しない**（DxO / OS が手動） |
| H3 UI | **「DxO修正後を記録」のみ**（コピー機能を付けない） |
| 痕跡の年の取り方 | Works 月フォルダ名の **先頭4文字を年**（`202606`→`2026`） |

---

## 2. オリジナル・フォルダ規則（正式）

文字位置の意味:

| 位置 | 意味 | 例 |
|------|------|-----|
| 1–2 | メーカー・機種コード `XX`（英大文字想定） | `OM`（OM SYSTEM）, `FF` など |
| 3–6 | 年 `YYYY` | `2026` |
| 7–8 | 月 `MM` | `06` |
| 9–10 | 日 `DD`（イベントのみ） | `15` |

フォルダ種別:

| 単位 | パターン | 例 |
|------|----------|-----|
| 年ルート | `XXYYYY` | `~/OM2026`, `~/FF2026` |
| 月 | `XXYYYYMM` | `~/OM2026/OM202606` |
| イベント | `XXYYYYMMDD_短い名前`（スペースなし） | `~/OM2026/OM202606/OM20260615_旅行` |

- `XX` は固定の1種類ではなく、**機種ごとに異なる2文字**を許す（同じ庫に `OM*` と `FF*` が混在してよい）。  
- 短絡の対象は **個別指定**（月直下とイベント配下は別バッチ）。  
- 月バッチは直下 JPEG のみ（イベント配下は含めない）。

**Works 月との対応（運用）:** 接頭辞2文字を除いた `YYYYMM`。  
例: `OM202606` → Works `~/2026/202606`。イベント由来の確定コマも同じ月 Works へ集約。

**実装メモ（P1）:** 現行 `library_unit` は接頭辞なしの `YYYYMM` / `YYYYMMDD_名前` のみ。  
→ **`XXYYYYMM` / `XXYYYYMMDD_名前`（`XX` = `[A-Za-z]{2}`）を正式に受付**するよう拡張する。  
　後方互換で接頭辞なし旧名も残すかは実装時に決める（推奨: 残す）。

---

## 3. Works 側のパス（ユーザー作成・月のみ）

```text
~/{YYYY}/{YYYYMM}/          # 例: ~/2026/202606
  （イベント用サブフォルダは置かない）
  *.jpg / *_dev.jpg         # 月直下バラ＋イベント由来もここに集約
```

- オリジナル `OM202606` / `FF202606` および配下イベントの確定コマは、すべて対応 Works 月（接頭辞を除いた `YYYYMM`）へ手動配置。  
- RAW は置かない。確定は Rating 3/4 の `_dev.jpg`（なければ撮って出し `.jpg`）。  
- 痕跡の対象フォルダは **常にこの月フォルダ**を選ぶ。

**シンプルになる点:** オリジナルのイベント単位と Works の月単位が分離され、「Works 側のイベント命名対応表」が不要になる。

---

## 4. アプリ責務の境界

| ゾーン | やること | やらないこと |
|--------|----------|--------------|
| **短絡** | M1–M3、監査、`DxO修正後を記録`（オリジナル月／イベント） | Works 生成、ファイルコピー、現像 |
| **痕跡** | Works **月**フォルダ上の JPEG にカード／ノート／ログ | コピー、イベント別 Works フォルダ |
| **DxO / OS** | Rating 修正、現像、Works 月フォルダへ手動配置 | — |

アプリ分割: **当面は1アプリ・2ゾーン**（短絡＋記録／痕跡）。

---

## 5. 「DxO修正後を記録」のタイミング（日またぎ現像）

- ボタンは **記録のみ**（オリジナル JPEG の Rating/説明を再スキャン → `post_h3` / `h3_delta`）。  
- **何度押してもよい**（現像途中の日でも可。最新スナップショットとして更新）。  
- Works へのコピーは **別作業（手動）**。記録ボタンの成否と独立。  
- 短絡作業を閉じるとき、未記録なら確認してよい（任意の UX。コピーは聞かない）。  
- 痕跡は **Works 月フォルダに実ファイルがあるとき**に実施。

---

## 6. 痕跡ログ配置（M1・簡略化後）

| 成果物 | 保存先 |
|--------|--------|
| 写真分析ノート・評価カード・月別ログ・処理ステータス | **Works 月フォルダ内** `~/{YYYY}/{YYYYMM}/` |
| 年別写真分析ログ | **Works 年フォルダ** `~/{YYYY}/写真分析ログ_{YYYY}.txt` |

例:

```text
~/2026/202606/
  202606写真分析ノート/
  202606評価カード/
  202606写真分析ログ.txt
  202606処理ステータス.txt
  P614_dev.jpg …
~/2026/写真分析ログ_2026.txt
```

### 年の決定・年次の置き場所

- 年番号 = 月フォルダ名の先頭4文字（`202606`→`2026`）。  
- 年次ファイルの親 = 月フォルダの直親（＝年フォルダ `2026`）。  

Works が月のみなら、現行 `DesktopLogManager`（`year_str = name[:4]`、年次＝`parent/`）が **このレイアウトと一致**する。  
→ **M1 の「祖先探索」改修は原則不要**。必要なら「痕跡対象は `YYYYMM` 名のみ」の検証・案内を足す程度。

---

## 7. 中優先改修への落とし込み（簡略化後）

| ID | 方針 |
|----|------|
| **M1** | **縮小:** Works＝`YYYYMM` 前提を文書化／任意で受付チェック。年次ログの特殊分岐は不要（現行で足りる） |
| **M2** | 「DxO修正後を記録」は **target 配下セッションのみ**を正とする |
| **M3** | 開始時に Tk 変数をスナップショットしてワーカーへ渡す |
| **M4** | 記録処理をワーカースレッド化（固まり防止）。コピー UI は作らない |
| **M5** | `card_theme` / `force_overwrite` は共有のまま文書化 |

### 上流タスク

| ID | 内容 |
|----|------|
| **P1** | `library_unit` / 短絡 GUI が `XXYYYYMM`・`XXYYYYMMDD_名前` を受け付ける（`XX`=機種2文字） |

---

## 8. やらないこと（再確認）

- オリジナル側イベントを月名型（`XXYYYYMM_名前`）のままにしない（正式は **`XXYYYYMMDD_名前`**）
- Works 上のイベントサブフォルダ運用
- Works フォルダの自動作成
- Works へのアプリによるファイルコピー／移動
- 「記録・コピー」一本化ボタン

---

## 9. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-11 | オーナー回答により確定。自動生成・アプリコピー案は撤回。ログ配置・記録 UI を固定 |
| 2026-08-11 | **Works は月 `YYYYMM` のみ**（イベントフォルダ非作成）。M1 を簡略化 |
| 2026-08-11 | オリジナル命名を一般化: **`XXYYYY` / `XXYYYYMM` / `XXYYYYMMDD_イベント`**（`XX`=機種コード） |
